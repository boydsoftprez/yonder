{
  "why": [
    "The widest honest value every reading on a page is photographed at (R-UI-23).",
    "",
    "The capture gate used to paint a grey box over every reading, because a load",
    "average is different on two runs and would leave the committed picture",
    "permanently dirty. That kept the file clean and it hid the defect the pictures",
    "exist to catch: a readout row is only ever as wrong as its *longest* value, and",
    "the value that happens to be on the page during a capture is never the longest",
    "one. `2000 kb/s` fits. `20000 kb/s`, which `schema/config.ts` allows, is what an",
    "operator's page actually has to hold — and nothing in this repository had ever",
    "drawn one.",
    "",
    "So each field is given one value here and the gate renders that instead of a",
    "mask. The picture is still identical on two runs, which is what the masking was",
    "for, and it is now a picture of the hardest case rather than of the easiest.",
    "",
    "**A page of specimens is not a board.** Every field is at its own widest, so",
    "the lamp can read NOTHING beside a sentence about a path that is carrying",
    "traffic. That is what the picture is for: it is not a portrait of one device",
    "in one state, it is the layout under the hardest content each of its fields",
    "can hold. The unmasked copy under `vendor/capture/` is the page as it really",
    "was, taken before any of this is written.",
    "",
    "**Honest means the value can actually arrive.** Every entry is derived from the code",
    "that emits it and names the line it was derived from: a bound in `schema/config.ts`, the",
    "widest branch of the function that composes the sentence, or the widest reading a real",
    "board has produced. Several were derived by *calling* that code rather than by typing",
    "the string out — `summarise()` for `What it offers`, `formatUptime` for `UP FOR`.",
    "A value invented to be long would fail the gate on a defect the console does not have,",
    "which costs the gate its credibility faster than missing one.",
    "",
    "**Where a field has no bound, the entry says so.** Several sentences are composed from",
    "strings nothing in this repository caps — a Node error inside a supervisor reason, a",
    "joined list of every by-path name a board can see, ModemManager's own failure word. For",
    "those the value here is the widest *producible* form, and its `why` says it is a floor",
    "and not a ceiling, so a number this gate prints is never read as the worst case when it",
    "is only the worst case anyone has written down.",
    "",
    "**A field is named by the instrument it is drawn in and the caption beside**",
    "**it** — `nrdb-ui-yonder-databar · UPLINK`. Not a DOM path and not a node id,",
    "because this file has to be checkable against the page by a person. Rename a",
    "caption and the specimen goes missing, loudly, which is the right failure: the",
    "widest honest value for UPLINK is not automatically the widest honest value for",
    "whatever it was renamed to.",
    "",
    "`masked` is the other half, and it is what stops this file from being a list of",
    "the easy ones. Every reading the gate finds is either specified here or listed",
    "there with a reason; a reading that is neither fails the build and names the key",
    "to add. An entry on either list that matches no reading on any page also fails —",
    "the same rule `accepted-violations.json` is held to, for the same reason.",
    "",
    "**No credential belongs in here (R-SEC-10).** These values are rendered into",
    "pages that are screenshotted and committed. The stream address resolves a real",
    "RTSP password on a real device, so the fixture carries a visibly fake one and",
    "the specimen for a stream address carries no password at all."
  ],
  "fields": {
    "nrdb-ui-yonder-databar · BOARD": {
      "value": "Raspberry Pi Compute Module 4 Rev 1.1",
      "why": "displayFacts' `model`, verbatim from /proc/device-tree/model. The Compute Module's string is the longest of the boards this project is built for."
    },
    "nrdb-ui-yonder-databar · UP FOR": {
      "value": "59 minutes 59 seconds",
      "why": "formatUptime (system/format.ts:46) prints the largest two units, which is not the same as the widest string: `365 days 23 hours` is 17 characters and this is 21. Derived by calling the built formatUptime(3599). A day count would need eight digits to beat it."
    },
    "nrdb-ui-yonder-databar · YONDER": {
      "value": "1.0.0-rc.1+build.42",
      "why": "the version out of package.json. A pre-release with build metadata is the longest semver this project can publish."
    },
    "nrdb-ui-yonder-databar · SYSTEM": {
      "value": "Debian GNU/Linux 12 (bookworm)",
      "why": "the OS version string, verbatim from /etc/os-release PRETTY_NAME on the distribution the installer targets."
    },
    "nrdb-ui-yonder-databar · OPERATOR": {
      "value": "Telefonica Moviles Espana SAU",
      "why": "the operator name ModemManager reports, passed through verbatim by the parser. Nothing in this repository bounds it, so this is a real registered long name rather than a cap."
    },
    "nrdb-ui-yonder-databar · NETWORK": {
      "value": "5GNR",
      "why": "formatTechnology, which uppercases ModemManager's identifier. `5gnr` is the longest of the ones it reports."
    },
    "nrdb-ui-yonder-databar · REGISTERED": {
      "value": "searching",
      "why": "the registration state, in ModemManager's own words. `searching` is the longest of home / roaming / searching / denied / idle."
    },
    "nrdb-ui-yonder-databar · APN": {
      "value": "internet.mnc001.mcc234.gprs",
      "why": "an APN as carriers publish them, out of config.yaml. `schema/config.ts` puts no length on it and 3GPP allows 100 characters, so this is a real operator-scoped name rather than a cap."
    },
    "nrdb-ui-yonder-databar · RSSI": {
      "value": "-113 dBm",
      "why": "the floor of the RSSI range a modem reports, which is the widest because it is the only three-digit one."
    },
    "nrdb-ui-yonder-databar · RSRQ": {
      "value": "-20 dB",
      "why": "formatDb (node-red-contrib-yonder-modem/src/format.ts:44) is `${Math.round(value)} dB`, so this field can never print a decimal and its width is a shape rather than a reading: a minus sign and two digits. LTE's own range bottoms out at -19.5 dB, which `Math.round` renders as -19, not -20 — JavaScript rounds a negative half towards zero. -20 is kept as the specimen because it is the same width and does not invite anyone to read a rounding rule out of it."
    },
    "nrdb-ui-yonder-databar · MTU": {
      "value": "9000",
      "why": "a jumbo frame. The link MTU is read from the interface and nothing caps it below this."
    },
    "nrdb-ui-yonder-databar · COMPOSITION": {
      "value": "MBIM",
      "why": "R-CEL-03's answer, one word, and MBIM is the longer of the two modes this device chooses between."
    },
    "nrdb-ui-yonder-databar · ADDRESS": {
      "value": "255.255.255.255",
      "why": "the widest IPv4 address. The link address on Status and on the Cellular tab is the same field asked twice."
    },
    "nrdb-ui-text.yonder-qualifier": {
      "value": "Ethernet is carrying traffic, and nothing has stood down",
      "why": "reachWhy's longest sentence, with the longest of PATH_WORDS' names (`ethernet`) raised by pathName."
    },
    "nrdb-ui-text": {
      "value": "The modem reported a failure: esim-without-profiles",
      "why": "modemState's widest branch (net/modem/state.ts:73), with the longest value ModemManager publishes for state-failed-reason. Nothing in this repository bounds that string — it is passed through verbatim from mmcli (mmcli/client.ts:140) — so this is a floor, not a ceiling. Drawn on the Interfaces tab and on the Cellular tab, which is why one specimen serves two widgets."
    },
    "nrdb-ui-text · Address": {
      "value": "255.255.255.255/32",
      "why": "the widest IPv4 address with its prefix length, which is the form NetworkManager reports."
    },
    "nrdb-ui-text · Remote": {
      "value": "Authorised, not reaching the network",
      "why": "messageFor's `label` (node-red-contrib-yonder-remote/src/state.ts). The longest of its seven phases."
    },
    "nrdb-ui-text · Status": {
      "value": "Authorised, not reaching the network",
      "why": "the same messageFor `label`, drawn again at the top of the ZeroTier tab."
    },
    "nrdb-ui-text · Path": {
      "value": "Relayed",
      "why": "R-VPN-03's two words, in words rather than a boolean. `Relayed` is the longer."
    },
    "nrdb-ui-text · Latency": {
      "value": "9999 ms",
      "why": "messageFor's `${ms} ms`. Four digits is a relayed hop over a congested cellular uplink, and the widest worth reporting."
    },
    "nrdb-ui-text · Network name": {
      "value": "Yonder field operations mesh",
      "why": "the name the mesh controller published. Nothing caps it, so this is the longest a controller has been configured with here."
    },
    "nrdb-ui-text · Network": {
      "value": "8286ac0e47d5f2c1",
      "why": "a ZeroTier network id, which is always sixteen hex characters."
    },
    "nrdb-ui-text · Traffic": {
      "value": "1023.9 GB in · 1023.9 GB out",
      "why": "messageFor's `traffic`, at formatBytes' top tier (GB) with the one decimal it prints from MB up."
    },
    "nrdb-ui-text · Throughput": {
      "value": "999.9 Gbps down · 999.9 Gbps up",
      "why": "messageFor's `throughput`, at formatRate's top tier (Gbps) with its one decimal. R-NET-10."
    },
    "nrdb-ui-text · Last heard": {
      "value": "59 min ago",
      "why": "formatLastHeard's longest branch. Minutes is where its digits and its unit are both widest."
    },
    "nrdb-ui-table · Time": {
      "value": "23:59:59",
      "why": "a clock, at the widest hour. Fixed width, so this is both the widest and the only honest value."
    },
    "nrdb-ui-table · Time (UTC)": {
      "value": "23:59:59",
      "why": "the same clock on the Log page, under its own column title."
    },
    "nrdb-ui-table · Level": {
      "value": "error",
      "why": "the longest of the levels the daemon writes: info, warn, error."
    },
    "nrdb-ui-table · What happened": {
      "value": "network: cellular is reaching the internet again at 2026-09-06T23:59:59.999Z; ethernet goes on carrying traffic and cellular is back in the running behind it",
      "why": "the widest line the daemon writes to its journal: standing.ts:247's recovery message with andThen's third branch (standing.ts:334), which is the only one naming three paths. 157 characters with the longest of PATH_WORDS. watch.ts:173's `network: could not check which way out is working (<message>)` is unbounded above."
    },
    "nrdb-ui-table": {
      "value": "network: cellular is reaching the internet again at 2026-09-06T23:59:59.999Z; ethernet goes on carrying traffic and cellular is back in the running behind it",
      "why": "the same journal line in the Activity table's second column, whose title is deliberately empty."
    },
    "nrdb-ui-yonder-databar · RUNNING": {
      "value": "failed — the pipeline could not be started: Error: spawn gst-launch-1.0 ENOENT; gave up after 5 restarts",
      "why": "runWords (video/present.ts:309) over the wider of the supervisor's two producers: supervisor.ts:206's `the pipeline could not be started: ${String(e)}` — not :205's exit code — plus the `; gave up after 5 restarts` suffix :191 adds at MAX_RESTARTS. `String(e)` is a Node error and is unbounded, so this is a floor."
    },
    "nrdb-ui-yonder-databar · IDENTITY": {
      "value": "platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.3:1.0-video-index0 — an enumeration number; it may mean a different camera after a reboot",
      "why": "identityWords' longer branch (video/present.ts) on the by-path name the bench board really answered — R-CAM-05's own sentence, drawn as a note cell on the readout strip. schema/config.ts caps `device` at 128 characters, so this is the widest observed rather than the widest permitted."
    },
    "nrdb-ui-yonder-databar · CAMERA": {
      "value": "Nose mast, forward of the airspeed probe",
      "why": "the camera's own name out of config.yaml (R-UI-27). `schema/config.ts` caps it at 48 characters and the Setup deck's field at 24; a hand-written configuration can carry the schema's, so that is the one a page has to hold. 40 characters of a name somebody would plausibly type, rather than 48 of filler."
    },
    "nrdb-ui-yonder-databar · PICTURE": {
      "value": "3840 × 2160",
      "why": "cameraStrip's `width × height` at the schema's ceilings (width max 3840, height max 2160)."
    },
    "nrdb-ui-yonder-databar · RATE": {
      "value": "60 fps",
      "why": "cameraStrip's `framerate fps` at the schema's ceiling of 60."
    },
    "nrdb-ui-yonder-databar · BITRATE": {
      "value": "20000 kb/s",
      "why": "cameraStrip's `bitrate_kbps kb/s` at the schema's ceiling of 20000. This is the defect spec §10 lists second: 2000 fits and 20000 does not."
    },
    "nrdb-ui-yonder-databar · UPLINK": {
      "value": "169.49 Mb/s at IP",
      "why": "cameraStrip's uplink: atIp(20000) × 8 outputs + atIp(4000) preview, at the schema's ceilings (outputs max 8, preview bitrate max 4000), in Mb/s to two places."
    },
    "nrdb-ui-yonder-databar · BEFORE YOU PRESS START": {
      "value": "this board has no /dev/v4l/by-path/platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.3:1.0-video-index0; the cameras it can see are platform-fd500000.pcie-pci-0000:01:00.0-usb-0:1.3:1.0-video-index0",
      "why": "cameraStrip's startCheck at refuse()'s widest branch, video/pipeline.ts:305 — not the SRT branch, which is 182 characters. This is its narrowest honest form: one camera, the bench's own 66-character by-path in both positions, 196 characters. It is unbounded above — schema/config.ts:562 caps `device` at 128 and `offered` joins every by-path name the board can see — so this is a floor for this field, not a ceiling."
    },
    "nrdb-ui-yonder-budget": {
      "value": "1356.0 of 5.0 Mb/s",
      "why": "YonderBudget's head line at the configuration schema/config.ts permits most of: eight cameras, eight outputs each, every one at the 20000 kb/s ceiling with a 4000 kb/s preview. Derived by calling the built uplinkBudget() on that configuration; the capacity is ASSUMED_UPLINK_KBPS (video/present.ts:46)."
    },
    "nrdb-ui-yonder-databar · UDP · RTP": {
      "value": "an outbound push to the configured ground station; it leaves over whichever path is active, cellular included",
      "why": "the widest of the four notes `video/receive.ts` gives an RTP output, derived by calling renderReceive() over every combination of paths, output states and credential it can be asked about (109 characters). The reachable one is the widest, which is worth knowing: the unusable sentences are not the hard case."
    },
    "nrdb-ui-yonder-databar · RTSP": {
      "value": "unusable — nothing can dial in to this RTSP listener over cellular; it is reachable on the mesh or a LAN",
      "why": "the widest of the five notes `video/receive.ts` gives an RTSP output, derived the same way (104 characters). This is the one an aircraft in flight actually draws, and R-UI-24 is the requirement it exists for."
    }
  },
  "masked": {
    "nrdb-ui-yonder-gauge · CPU LOAD": {
      "why": "A gauge draws the same number twice — once as text and once as a needle — and this gate can move only one of them. A specimen here would commit a picture of a needle at one value with another value written beside it, which is a worse record than a grey box. The gauges are the one instrument whose reading cannot be substituted from outside; a widest-value gauge has to be driven through the daemon."
    },
    "nrdb-ui-yonder-gauge · CPU TEMP": {
      "why": "As CPU LOAD: text and needle, and only the text can be written from here."
    },
    "nrdb-ui-yonder-gauge · MEMORY": {
      "why": "As CPU LOAD: text and needle, and only the text can be written from here."
    },
    "nrdb-ui-yonder-gauge · SIGNAL": {
      "why": "As CPU LOAD: text and needle, and only the text can be written from here."
    },
    "nrdb-ui-yonder-gauge · QUALITY": {
      "why": "As CPU LOAD: text and needle, and only the text can be written from here."
    },
    "nrdb-ui-yonder-annunciator.yonder-live.yonder-pending": {
      "why": "The confirmation countdown. It has a widest honest value — the full window, at three digits — and writing it would put CHANGE PENDING on every page captured with nothing pending, which is the one thing about this annunciator a picture has to get right. The pending captures (status-pending, network-cellular-pending) exist to photograph the other state, and they are the reason a single specimen cannot be honest for both."
    },
    "nrdb-ui-text · Host": {
      "why": "Nothing composes a value for this field yet. The widget binds {{msg.payload}} straight from yonder-diag, which sends the daemon's whole reply object, so the field renders nothing until a probe runs and an object afterwards. A specimen would state a format this console does not have."
    },
    "nrdb-ui-text · Replies": {
      "why": "As Host: bound to the whole yonder-diag payload, with no composed value to be the widest of."
    },
    "nrdb-ui-text · Round trip (ms)": {
      "why": "As Host: bound to the whole yonder-diag payload, with no composed value to be the widest of."
    },
    "nrdb-ui-text · Probed": {
      "why": "As Host: bound to the whole yonder-diag payload, with no composed value to be the widest of."
    }
  }
}
